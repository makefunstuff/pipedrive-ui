import * as $ from 'jquery';
import * as Backbone from 'backbone';
import * as _ from 'underscore';

import {tagName, template, on} from '../utils/decorators';

@tagName('div')
class Dashboards extends Backbone.View {
  render() {
  }
}
